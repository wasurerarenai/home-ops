machine:
  install:
    extraKernelArgs:
      - i915.enable_guc=3
  nodeLabels:
    intel.feature.node.kubernetes.io/gpu: "true"
  udev:
    rules:
      - # Intel GPU
        SUBSYSTEM=="drm", KERNEL=="renderD*", GROUP="44", MODE="0660"
